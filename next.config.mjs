/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add the output property to specify the export directory
  output: {
    // Specify the export directory
    dir: 'out', // Change 'out' to your desired export directory
  },
};

export default nextConfig;
